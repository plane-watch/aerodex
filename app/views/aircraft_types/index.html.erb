<% content_for :page_title, 'Aircraft Types' -%>
<% content_for :page_description, 'View and search aircraft types here. Use the search bar above to search by name, type code, manufacturer and more!' -%>
<%= content_for :search do %>
  <%= render 'shared/search/field_search_form',
             url: aircraft_types_path,
             model_name: 'AircraftType',
             placeholder: 'Search Aircraft Types...',
             tokens: parsed_search_tokens %>
<% end %>

<div class="mt-8 flow-root">
  <div class="-mx-4 -my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
    <div class="inline-block min-w-full py-2 align-middle sm:px-6 lg:px-8">
      <table class="min-w-full divide-y divide-gray-300">
        <thead>
        <tr>
          <th scope="col" class="py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-gray-900 sm:pl-0">
            Type
          </th>
          <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
            Type Code
          </th>
          <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
            Manufacturer
          </th>
          <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900 hidden lg:block">
            Category
          </th>
          <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
            Engines
          </th>
          <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-gray-900">
            Aircraft Count
          </th>
          <th scope="col" class="relative py-3.5 pl-3 pr-4 sm:pr-0">
            <span class="sr-only">Edit</span>
          </th>
        </tr>
        </thead>
        <tbody class="divide-y divide-gray-200 bg-white" id='list'>
        <%= render partial: 'aircraft_type', collection: @aircraft_types %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<% if @pagy.pages > 1 %>
  <div data-controller="infinite-scroll"
       data-infinite-scroll-container-target-value="list"
       data-infinite-scroll-url-value="<%= aircraft_types_path %>">
  </div>
<% end %>
<div class="relative">
  <div class="absolute inset-0 flex items-center" aria-hidden="true">
    <div class="w-full border-t border-gray-300"></div>
  </div>
  <div class="relative flex justify-center">
    <span class="bg-white px-2 text-sm text-gray-500">EOF</span>
  </div>
</div>
<div class="w-full mt-5 text-center">
  <span class="text-gradient">
    Welp, we've run out of aircraft types to show you.
    <span class="font-bold text-shadow-lg shadow-fuchsia-300">Maybe you can help us add some more?</span>
  </span>
</div>